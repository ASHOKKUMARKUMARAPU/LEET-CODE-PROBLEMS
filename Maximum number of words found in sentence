class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        res = []
        sub = []

        for i in range(0,len(sentences)):
            sub = len(sentences[i].split(' '))
            res.append(sub)
        return max(res)
